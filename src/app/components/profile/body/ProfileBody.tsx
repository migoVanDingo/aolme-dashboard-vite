import React from 'react'
import styled from 'styled-components'

const SBodyContainer = styled.div`
    
`

const ProfileBody = ({}: any) => {
  return (
    <SBodyContainer>ProfileBody</SBodyContainer>
  )
}

export default ProfileBody